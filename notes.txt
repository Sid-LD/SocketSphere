-u make a link btw sender id and receiver id and then make a room with that link and then send the message to that room so that only those two can see the message
-parse the json data that is coming from the client side
-app.use(cors({origin:FRONTEND_URL, credentials:true}))=> allow the client to send auth cookies and headers with the request
- render becomes inactive after 15 mins so we send scheduled task everyy 14th min

-signing secrets- mongodb has to ensure all the request comes from clerk

-when u want to listen to events use socket.on()

npm i react-router
npm i axios socket.io-client
npm install react-hot-toast

#Zustand
->used in state management
->suppose we have a state in parent component we need to passs the state in the child component we need to prop drill all the component(go sequentially from one component to the next
. with zustand u create a gloval central container where u keep all ur func n methods and any component can access that global container)

Zustand stores state centrally and notifies only the components that subscribe to the changed value